What are 4 commonly used logic components and when would you use each one?

Dropdown
Checkboxes
Radio Button
Decision

679b806cccbf95675783e7cd - Subbmission ID
679b80bfa5cfc886c34a77c1
679b80f4a5cfc886c34a7882

#For finding data in console enter this
The Unqork Angular command angular.element('.unqorkio-form').scope().submission allows you to inspect the JSON object in the Express view. In order to use it, hold CTRL + SHIFT + J (Windows)/CMD + SHIFT + J (Mac) to open the Inspector console. Click the Console tab. Paste the angular command and hit Enter. Opening the data object can provide insight into what data is present in the browser
angular.element('.unqorkio-form').scope().submission.data


Unqork’s “Execute via Proxy” internal services
The four internal services we will be using are:
Execute via Proxy - GET
Execute via Proxy - POST
Execute via Proxy - PUT
Execute via Proxy - DELETE
Each of service will be used by a plugin component and a calculator will be used set the conditions to fire any of the services


Role - Admin - CRUD User , U Plants
Role - User - CRUD Plants, RU User

Table - Users
Name, Location, Status(Active, Deactive)
Table- Plants
Name, Location, 


?assumeRole={role}
?preview=true&assumeRole=Authenticated




{
    "_request":{
        "method":"POST",
        "path": "/v1/users",
        "query": {},
        "body": {
               "userId":"u001",
                "firstName": "Post",
                "lastName": "Name"
}
      }
 }


{
    "_request":{
        "method":"GET",
        "path": "/v1/users",
        "query": {},
        "body": {
              
}
      }
 }

{
    "_request":{
        "method":"PUT",
        "path": "/v1/users/u003",
        "query": {},
        "body": {
               "firstName":"PUT test"
}
      }
 }


{
    "_request":{
        "method":"DELETE",
        "path": "/v1/users",
        "query": {
		"ids":"u003,u002"
},
        "body": {
}
      }
 }

